<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>IdeaBox</title>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap-theme.min.css">
    <link rel="stylesheet" href="style.css">
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <%= erb :notices %>
    <div class="container">
      <div class="jumbotron">
        <h1>IdeaBox</h1>
        <p>A place to store bad ideas.</p>
      </div>
      <div class="row">
        <div class="col-xs-12 sub-margin">
          <% if @user %>
            <div class="pull-right">
              <span id="username">Welcome, <b><%= @user.username %></b></span>
              |
              <a href="/logout">Log Out</a>
            </div>
          <% else %>
            <div class="login-register">
              <button class="btn btn-primary btn-md pull-right" data-toggle="modal" data-target="#register">
                Register
              </button>
              <button class="btn btn-primary btn-md pull-right" data-toggle="modal" data-target="#login">
                Log In
              </button>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <%= yield %>

    <%= erb :login_modal %>
    <%= erb :register_modal %>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  </body>
</html>
